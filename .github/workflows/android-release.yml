name: Build & Release APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    name: Build & Create Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant Execute Permission for Gradlew
        run: chmod +x gradlew

      - name: Restore Keystore for Consistent Signing
        run: |
          mkdir -p ~/.android
          echo "${{ secrets.DEBUG_KEYSTORE_BASE64 }}" | base64 -d > debug.keystore
          cp debug.keystore app/debug.keystore
          cp debug.keystore ~/.android/debug.keystore
          echo "Successfully restored consistent debug.keystore to root, app/, and ~/.android/"

      - name: Determine App Version
        id: versioning
        run: |
          VERSION_NAME="0.1.${{ github.run_number }}"
          VERSION_CODE="${{ github.run_number }}"
          VERSION_TAG="v${VERSION_NAME}"
          echo "VERSION_NAME=$VERSION_NAME" >> $GITHUB_OUTPUT
          echo "VERSION_CODE=$VERSION_CODE" >> $GITHUB_OUTPUT
          echo "VERSION_TAG=$VERSION_TAG" >> $GITHUB_OUTPUT
          echo "Generated version: $VERSION_NAME (Code: $VERSION_CODE, Tag: $VERSION_TAG)"

      - name: Build Debug & Release APK with Version
        env:
          APP_VERSION_NAME: ${{ steps.versioning.outputs.VERSION_NAME }}
          APP_VERSION_CODE: ${{ steps.versioning.outputs.VERSION_CODE }}
          GITHUB_REPO: ${{ github.repository }}
        run: |
          ./gradlew assembleDebug assembleRelease testDebugUnitTest \
            -PversionName="${{ steps.versioning.outputs.VERSION_NAME }}" \
            -PversionCode="${{ steps.versioning.outputs.VERSION_CODE }}" \
            -PgithubRepository="${{ github.repository }}" \
            --stacktrace

      - name: Prepare Release Artifacts
        run: |
          mkdir -p release-artifacts
          cp app/build/outputs/apk/debug/app-debug.apk release-artifacts/neptun-mobile-${{ steps.versioning.outputs.VERSION_NAME }}.apk
          cp app/build/outputs/apk/debug/app-debug.apk release-artifacts/app-debug.apk
          if [ -f app/build/outputs/apk/release/app-release.apk ]; then
            cp app/build/outputs/apk/release/app-release.apk release-artifacts/neptun-mobile-${{ steps.versioning.outputs.VERSION_NAME }}-release.apk
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.versioning.outputs.VERSION_TAG }}
          name: "Neptun Mobile ${{ steps.versioning.outputs.VERSION_TAG }}"
          body: |
            ## Neptun Mobile ${{ steps.versioning.outputs.VERSION_TAG }}
            
            Automatikusan kiadott új verzió a(z) `${{ github.sha }}` commitból.
            
            ### Változtatások és Verzióinformációk:
            - **Alkalmazás verzió (versionName):** `${{ steps.versioning.outputs.VERSION_NAME }}`
            - **Build kód (versionCode):** `${{ steps.versioning.outputs.VERSION_CODE }}`
            - **Csomagnév (package):** `app.neptun.mobile`
            - **Commit:** `${{ github.sha }}`
            
            ### Letölthető telepítőfájlok:
            - [`neptun-mobile-${{ steps.versioning.outputs.VERSION_NAME }}.apk`](https://github.com/${{ github.repository }}/releases/download/${{ steps.versioning.outputs.VERSION_TAG }}/neptun-mobile-${{ steps.versioning.outputs.VERSION_NAME }}.apk)
            - [`app-debug.apk`](https://github.com/${{ github.repository }}/releases/download/${{ steps.versioning.outputs.VERSION_TAG }}/app-debug.apk)
          files: |
            release-artifacts/neptun-mobile-${{ steps.versioning.outputs.VERSION_NAME }}.apk
            release-artifacts/app-debug.apk
            release-artifacts/neptun-mobile-${{ steps.versioning.outputs.VERSION_NAME }}-release.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
